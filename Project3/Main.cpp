//PSSI_Studio projects

#include "GameEngine.h"

int main() {
    GameEngine engine;
    engine.run();
    return 0;
}

//GameEngine.cpp - The main game logic, it runs the physics the UI and over all every element!
//Button.cpp - This is the most important button which is the close button! It handles it's events!
//GameEngine.h- This is for storing the variables for the game Engine so the files doesn't become too long!
//Button.h - Used for the same purpose!
//SFML - Mainly used!